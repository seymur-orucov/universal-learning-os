# Learner Snapshot Example

This example is concise, practical, and safe to store. It does not include sensitive personal data and does not require daily repository updates.

## Snapshot Type

Milestone summary

## Domain

SQL/PostgreSQL

## Date

2026-07-09

## Scope

Beginner filtering milestone covering basic `SELECT`, column selection, and simple `WHERE` conditions.

## Completed Topics

- Basic `SELECT ... FROM ...` structure.
- Selecting specific columns.
- Simple `WHERE` filters.
- Basic comparison operators.
- String literal quoting in filters.

## Current Strengths

- Writes simple `SELECT` queries with a clear table source.
- Understands basic row filtering with one condition.
- Can explain why text values need quotes.

## Current Weak Areas

- Needs more practice with `AND` / `OR` combinations.
- Needs review of parentheses in compound conditions.
- Has not yet demonstrated `NULL` filtering.

## Recommended Next Actions

- Practice three filtering tasks using `AND`, `OR`, and parentheses.
- Introduce `IS NULL` and `IS NOT NULL` after compound filters are reviewed.
- Review mistakes before moving to sorting with `ORDER BY`.

## Source Note

Learner-requested milestone snapshot based on recent SQL/PostgreSQL chat practice. This is a summary, not a full transcript.

## Optional Evidence Summary

Include only for explicit progress or snapshot use.

- Evidence reviewed: one learner-written `WHERE` query with a single string condition.
- Evidence limitation: not enough evidence yet for independent compound filtering or `NULL` handling.
