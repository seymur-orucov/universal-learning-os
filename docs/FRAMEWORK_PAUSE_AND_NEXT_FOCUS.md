# Framework Pause and Next Focus

The current framework cycle is feature-complete enough for real use. The next focus is actual learning, not more infrastructure.

```text
ChatGPT Project = learning
Codex/repo = framework maintenance
Studio = optional local control panel
```

Studio development is paused. Do not add new domains unless explicitly requested, and do not expand the app until real learning pilots show a clear need. The CLI and generated packs are stable enough for current usage; the learning stack can now grow through real ChatGPT Projects.

```text
Do not build more framework features until at least SQL/PostgreSQL and English pilots are completed.
```

Normal learning does not require a repository update after every lesson. Handoffs and snapshots remain optional and user-requested. The generated packs remain learner-neutral, while daily progress can stay in the chat.
