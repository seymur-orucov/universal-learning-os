# SQL/PostgreSQL Review Queue

| Review Item | Priority | Reason | Evidence Status | Suggested Review Task |
| --- | --- | --- | --- | --- |
| `SELECT` basics | High | Prior work exists but evidence is incomplete | not enough evidence | Write simple `SELECT` queries and explain selected columns. |
| `WHERE` filtering | High | Core prerequisite for advanced filtering | not enough evidence | Filter rows with comparison and boolean conditions. |
| `ORDER BY` | Medium | Prior practice needs verification | not enough evidence | Sort results by one and multiple columns. |
| `LIMIT/OFFSET` | Medium | Prior practice needs verification | not enough evidence | Paginate result sets and explain ordering dependency. |
| `DISTINCT` | Medium | Prior practice needs verification | not enough evidence | Remove duplicates and explain when duplicates appear. |
| `IN / NOT IN` | High | Requested advanced filtering topic | not enough evidence | Compare multiple values and explain `NULL` caveats. |
| `BETWEEN` | High | Requested advanced filtering topic | not enough evidence | Filter ranges and explain inclusive boundaries. |
| `LIKE / ILIKE` | High | Requested advanced filtering topic | not enough evidence | Match text patterns and explain case sensitivity. |
| `IS NULL / IS NOT NULL` | High | Requested advanced filtering topic | not enough evidence | Filter missing values and explain why `= NULL` is wrong. |
