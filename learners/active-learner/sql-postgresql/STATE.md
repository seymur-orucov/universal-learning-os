# SQL/PostgreSQL Learner State

## Active Project

SQL/PostgreSQL Mentor OS

## Domain

`sql-postgresql`

## Current Track

Beginner-to-advanced SQL/PostgreSQL

## Learner Goal

Become strong in SQL and PostgreSQL, from beginner to advanced, including practical database work, Northwind tasks, normalization, indexing, transactions, MVCC, `EXPLAIN/ANALYZE`, and query optimization.

## Known Progress Summary

- Early SQL lessons were started.
- `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`, `DISTINCT`, and `OFFSET` were practiced in prior learning context.
- An advanced filtering lesson around `IN`, `NOT IN`, `BETWEEN`, `LIKE`, `ILIKE`, `IS NULL`, and `IS NOT NULL` was requested.
- Exact mastery is not proven unless evidence exists in session summaries.

## Mastery Status

- Early SQL basics: not enough evidence.
- Advanced filtering: not enough evidence.
- Joins and later topics: not enough evidence.

## Current Recommended Next Action

Perform `REVIEW` or `ASSESS` for early SQL basics before continuing, because prior chat may be incomplete or deleted.

## Localization

- Azerbaijani explanations.
- SQL/PostgreSQL terms in English where natural.

## Learner State Warning

Do not treat lesson exposure as mastery. Mastery claims require evidence or explicit user instruction.
