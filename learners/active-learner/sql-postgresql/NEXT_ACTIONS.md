# SQL/PostgreSQL Next Actions

## Recommended Next Command

`REVIEW` or `ASSESS`

## Reason

Deleted or incomplete previous chat means evidence should be verified before advancing.

## Next Lesson Option After Review

- Continue advanced filtering with `IN`, `NOT IN`, `BETWEEN`, `LIKE`, `ILIKE`, `IS NULL`, and `IS NOT NULL`.
- Move to joins only if early SQL basics are sufficiently demonstrated or the learner explicitly overrides.

## Ready-to-Paste Next Prompt

```text
Continue using SQL/PostgreSQL Mentor OS.

Instruction language: Azerbaijani.
Keep SQL/PostgreSQL terminology in English where natural.

Use learners/active-learner/sql-postgresql/STATE.md as the current learner state summary.

Recommended command: REVIEW or ASSESS.
Reason: prior chat may be incomplete/deleted, so verify evidence for SELECT, WHERE, ORDER BY, LIMIT, DISTINCT, OFFSET, and advanced filtering before continuing.

Do not assume mastery. If evidence is missing, say "not enough evidence".

Start with a short review or assessment of early SQL basics, then recommend whether to continue advanced filtering or move toward joins.
End with session summary, evidence generated, proposed state updates, next action, and next chat continuation prompt.
```
