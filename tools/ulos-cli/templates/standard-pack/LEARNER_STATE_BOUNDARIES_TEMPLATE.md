# Learner State Boundaries Template

Expected output file: `LEARNER_STATE_BOUNDARIES.md`

Purpose: Summarize what generated packs may and may not do with learner state.

Source files:
- `specification/STATE_SPEC.md`
- `learners/README.md` when present
- learner state templates when selected

Placeholders: `{{DOMAIN_ID}}`, `{{DOMAIN_TITLE}}`, `{{PROFILE}}`, `{{SOURCE_CONTENT}}`

Template body:

```text
# Learner State Boundaries

{{SOURCE_CONTENT}}
```
