# Git Project Setup

## Project Name

Git Mentor

Create one dedicated ChatGPT Project. Use `exports/generated/git-standard/` when the 25-file profile fits. Paste `PROJECT_INSTRUCTIONS.md` into Project Instructions, upload the remaining pack files, and start with `GIT_STARTUP_PROMPT_READY.md`.

Use disposable repositories for destructive or recovery practice. Keep learner state separate and use handoff or snapshot helpers only when explicitly requested. Studio remains optional.
