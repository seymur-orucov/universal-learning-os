# Schemas

This directory is reserved for future JSON Schema files.

JSON Schema files SHOULD validate machine-readable artifacts that trace back to canonical Markdown specifications in `specification/`.

## Current Status

- No schema files exist yet.
- No validation command exists yet.

## OPEN QUESTION

- Which specification should receive the first machine-readable schema?

